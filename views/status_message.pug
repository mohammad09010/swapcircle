extends layout

block content
  .hero
    h2= title
    p= message
    .actions
      a.btn(href=primaryLink)= primaryText
      if secondaryLink && secondaryText
        a.btn.btn-ghost(href=secondaryLink)= secondaryText
