extends layout

block content
  .card
    h2 Tags / Categories
    p Click a tag to see matching items.

    if tags.length
      ul
        each t in tags
          li: a(href=`/tags/${t.tag_id}/items`)= t.name
    else
      p No tags found (check DB init scripts ran).
